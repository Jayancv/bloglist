name: Health Check

on:
  schedule:
  - cron: '*/5 * * * *'
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  health_check:
    runs-on: ubuntu-24.04
    steps:
      - name: Check app health
        uses: Jtalk/url-health-check-action@v4
        with:
          url: https://bloglist-dop5.onrender.com/health
          max-attempts: 3
          retry-delay: 30s
          retry-all: true